-- ---------------------------------------------------------------------------
--  Notes: Aurora Custom Ability List
--  Provoke 20s
-- ---------------------------------------------------------------------------

UPDATE `abilities` SET recastTime='20' WHERE name='provoke';
